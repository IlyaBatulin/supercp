# Скопируйте в `.env`. Переменные подхватываются при запуске main.py / gui.py.

LAB_LOGIN=student
LAB_PASSWORD=lab123
SUPERCOMPUTER_ACCESS_KEY=super-key-demo

# Пароли для шага «доступ к узлу» (id узлов: alpha, beta, gamma — см. main.py)
NODE_PASSWORD_ALPHA=node-alpha-1
NODE_PASSWORD_BETA=node-beta-1
NODE_PASSWORD_GAMMA=node-gamma-1

# Опционально по ТЗ
# SECRET_INTEGRATION_1=
# SECRET_INTEGRATION_2=
# SECRET_INTEGRATION_3=

# Настройки Django/Replit
DJANGO_SECRET_KEY=change-me-for-replit
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=*
PORT=8000
